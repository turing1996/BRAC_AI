# Data split manifests

This directory is reserved for de-identified sample/patient split manifests when sharing is permitted.

Recommended files:

```text
tcga_train.csv
tcga_validation.csv
cbcga_external.csv
```

At minimum, include a stable non-identifying sample key and patient key sufficient to verify patient-level separation. Do not upload protected clinical identifiers.
